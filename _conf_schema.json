{
  "projects": {
    "description": "项目接入列表：一条配置表示一个 QQ 群绑定一个 GitHub 项目。",
    "type": "template_list",
    "default": [],
    "hint": "每条绑定至少填写 QQ群号 和 Git 仓库地址。插件只会在对应群聊里监听并回答该项目问题。",
    "templates": {
      "project": {
        "name": "QQ群项目绑定",
        "display_item": "repo_url",
        "items": {
          "enabled": {
            "description": "启用此项目绑定",
            "type": "bool",
            "default": true
          },
          "group_id": {
            "description": "QQ群号",
            "type": "string",
            "default": "",
            "hint": "只填群号，例如 123456789。"
          },
          "project_prompt": {
            "description": "项目简介提示词",
            "type": "text",
            "default": "",
            "hint": "简单介绍这个项目是做什么的、常见使用场景、哪些问题算本项目问题、哪些问题通常不属于本项目。会注入给 Agent 帮助判断是否需要回复。"
          },
          "repo_url": {
            "description": "Git 仓库地址（必填）",
            "type": "string",
            "default": "",
            "hint": "填写 GitHub/Git 仓库地址，例如 https://github.com/owner/repo.git。未填写时这条绑定不会生效。",
            "obvious_hint": true
          },
          "repo_branch": {
            "description": "可选分支、tag 或 commit",
            "type": "string",
            "default": "",
            "hint": "留空时自动优先使用 main，其次 master；如果两者都不存在，则使用远端默认分支。"
          },
          "repo_path": {
            "description": "本地仓库 checkout 路径",
            "type": "string",
            "default": "",
            "hint": "留空时按 GitHub 仓库名自动生成；相对路径会放到 data/plugin_data/astrbot_plugin_project_helper/repos。"
          },
          "qa_path": {
            "description": "项目 QA Markdown 路径",
            "type": "string",
            "default": "",
            "hint": "留空时按 GitHub 仓库名自动生成；相对路径会放到 data/plugin_data/astrbot_plugin_project_helper/qa。"
          }
        }
      }
    }
  },
  "admin_qqs": {
    "description": "错误通知管理员 QQ 号列表",
    "type": "list",
    "default": [],
    "hint": "当 Agent 或仓库处理失败时，插件会私聊这些 QQ 号；不会把错误发到群里。"
  },
  "buffer_seconds": {
    "description": "等待连续消息结束的秒数",
    "type": "float",
    "default": 15.0,
    "hint": "每来一条新消息会重新计时；适当调大可以避免把同一个问题切碎。"
  },
  "max_buffer_messages": {
    "description": "一次聚合最多保留的消息条数",
    "type": "int",
    "default": 20,
    "hint": "超过后会丢弃最早消息并在 Agent 提示里说明发生了截断。"
  },
  "max_answer_chars": {
    "description": "单次群回复最大字符数",
    "type": "int",
    "default": 700,
    "hint": "默认更适合群聊短回复；如果确实要详细教程可以调大。"
  },
  "answer_style_prompt": {
    "description": "群聊回复语气提示词",
    "type": "text",
    "default": "像项目群里一个熟悉项目的真人群友在顺手回复。默认 1 到 4 句话，除非用户明确要教程，不要写长篇编号列表。先给结论或下一步操作，再补一句原因；语气轻松、直接、有边界，不要客服腔。可以说“先看这个”“一般是这样”“这个不用重抓整站”，不要说“建议您按照以下步骤”。示例好回复：先看缺邮箱候选有没有个人主页链接。有的话在抓取审核页选中它，点补全导师资料，系统会进详情页继续找邮箱。示例差回复：好的，这个问题明显是项目相关的，我来回答。常见原因如下：1...2...3...",
    "hint": "可按你的群风格微调。这个提示词会注入给 Agent，只影响回答语气和长度，不影响仓库工具权限。"
  },
  "agent_timeout_seconds": {
    "description": "单次 Agent 总超时秒数",
    "type": "float",
    "default": 300.0
  },
  "tool_timeout_seconds": {
    "description": "单个工具调用超时秒数",
    "type": "int",
    "default": 30
  },
  "max_tool_calls": {
    "description": "单次 Agent 最多工具调用轮数",
    "type": "int",
    "default": 25,
    "hint": "用于防止 Agent 无限循环；复杂项目可适当调大。运行时会限制在 1..80。"
  },
  "conversation_ttl_seconds": {
    "description": "缓冲消息最长保留秒数",
    "type": "float",
    "default": 300.0
  },
  "respond_when_mentioned_only": {
    "description": "仅在唤醒或 @ 机器人时处理消息",
    "type": "bool",
    "default": false
  },
  "send_typing": {
    "description": "显示机器人正在输入状态",
    "type": "bool",
    "default": true,
    "hint": "开启后，Agent 调查期间会显示平台的“正在输入”状态；不会额外向群里发送文字消息。"
  },
  "auto_update_repo": {
    "description": "每次回答前自动 git fetch/pull",
    "type": "bool",
    "default": true
  },
  "include_sources": {
    "description": "要求 Agent 在必要时附上简短文件依据",
    "type": "bool",
    "default": false,
    "hint": "默认关闭，群友一般只需要产品侧结论；开启后可能会在回答末尾附少量文件路径。"
  },
  "error_notify_admins": {
    "description": "处理失败时私聊管理员",
    "type": "bool",
    "default": true,
    "hint": "需要同时配置 admin_qqs。关闭后失败只会写入 AstrBot 日志。"
  }
}
